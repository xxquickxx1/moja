<div class="home-container">
<div class="row-fluid">

<div class="span12 white padding-20">
<span class="pull-left"><h5>Messages</h5></span><span class="pull-right"><%= link_to 'Back', dashboard_path %></span>
<br />  
<hr />

<% @messages.each do |message| %>

<%= link_to "#{message.subject}", '/users/message/' + "#{message.id}" %>



<% end %>

<% if @messages.size < 1 %>

<strong>You have no messages</strong>

<% end %>


</div>
</div>
</div>

